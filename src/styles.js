import styled from "styled-components";

const colors = {
  headerBackground: "#79BEEE",
  background: "#B7D3F2"
};
export const Container = styled.div`
  display: flex;
  flex: 1;
`;

export default colors;
